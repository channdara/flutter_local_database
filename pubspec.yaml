name: learning_local_database
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  sqflite: ^1.1.6
  path_provider: ^1.1.2
  shared_preferences: ^0.5.3+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/image/

  fonts:
    - family: ProductSans
      fonts:
        - asset: assets/font/product_sans_regular.ttf